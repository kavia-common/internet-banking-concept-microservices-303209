apiVersion: v1
kind: Service
metadata:
  name: keycloak
  namespace: internet-banking
  labels:
    app: keycloak
    tier: security
spec:
  type: ClusterIP
  ports:
  - port: 8080
    targetPort: 8080
    protocol: TCP
    name: http
  selector:
    app: keycloak
